---
question: How do I configure the site?
category: Getting Started
order: 1
tags:
  - configuration
---

Edit `config.yaml` in your docs directory. See the [configuration guide](../getting-started/configuration.md) for all available options.

<!--
FRONTMATTER REFERENCE:

question: (required) The FAQ question text
category: (required) Category name for grouping (matches directory name or overrides it)
order: (optional) Sort order within category (default: 0)
tags: (optional) Tags for search

DIRECTORY:
Place in docs/__faq__/category-name/question-slug.md
The directory name becomes the default category.
Enable FAQ in config.yaml:
  faq:
    enabled: true
-->
